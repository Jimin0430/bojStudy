#pragma warning(disable:4996)
#include<stdio.h>
int main()
{
	int hour, min;
	scanf("%d %d", &hour, &min);
	if (min >= 45)
		printf("%d %d", hour, min - 45);
	else
	{
		if (hour != 0)
			printf("%d %d", hour - 1, min + 15);
		else
			printf("%d %d", hour + 23, min + 15);
	}
		
	
	return 0;
}
